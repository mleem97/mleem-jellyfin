# Spotify Cover & Metadata Provider

Primary artwork for `MusicAlbum` and `MusicArtist` via the Spotify Web API.

- Client Credentials authentication with cached bearer tokens
- Largest available image per album/artist
- Admin status endpoint: `GET /Plugins/SpotifyProvider/Status`
